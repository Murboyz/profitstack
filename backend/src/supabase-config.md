# Supabase Wiring Notes

Local env file used:
- `profitstack/.env.local`

Required vars:
- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `DATABASE_URL`

Do not commit real secrets.
Use `.gitignore` to keep env files local-only.
